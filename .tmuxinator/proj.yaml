name: <%= @args[0] %>
root: ~/<%= @args[0] %>
windows:
  - project:
      layout: main-horizontal
      panes:
        - vim .
        -  # just bash
        -  # just bash
  - extra-terminals:
      panes:
        -  # just bash
